import 'package:flutter/material.dart';

class AppColors {
  // Light mode colors similar to Instagram
  static const Color primary = Color(0xFFFAFAFA); // Light background
  static const Color secondary = Color(0xFF000000); // Text color
  static const Color accent = Color(0xFF0095F6); // Instagram blue
  static const Color grey = Color(0xFFBDBDBD);

  // Dark mode colors similar to Instagram
  static const Color darkPrimary = Color(0xFF000000); // Instagram's dark theme
  static const Color darkAuth = Color(0xFF1C1C1E); // Instagram's dark theme for authentication screens
  static const Color darkSecondary = Color(0xFFFFFFFF); // Text color
  static const Color darkAccent = Color(
    0xFF0095F6,
  ); // Instagram's blue in dark mode
}
